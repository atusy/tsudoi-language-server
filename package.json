{
  "name": "@atusy/tsudoi-workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "test:unit": "bun test packages scripts/src",
    "test:integration": "bun test tests/integration",
    "test:e2e": "bun test tests/e2e",
    "lint": "oxlint --format unix --deny-warnings --report-unused-disable-directives-severity error",
    "fmt:check": "oxfmt --check .",
    "typecheck": "bun run scripts/typecheck-workspaces.ts && tsc --noEmit",
    "check": "bun run test:unit && bun run test:integration && bun run test:e2e && bun run lint && bun run fmt:check && bun run typecheck"
  },
  "devDependencies": {
    "@atusy/tsudoi-adapter-efm-config": "0.1.0-alpha.2",
    "@atusy/tsudoi-completion-dictionary": "0.1.0-alpha.2",
    "@atusy/tsudoi-completion-document": "0.1.0-alpha.2",
    "@atusy/tsudoi-completion-path": "0.1.0-alpha.2",
    "@atusy/tsudoi-completion-shell": "0.1.0-alpha.2",
    "@atusy/tsudoi-hover-wordnet": "0.1.0-alpha.2",
    "@atusy/tsudoi-language-server": "0.1.0-alpha.2",
    "@types/bun": "^1.3.0",
    "@types/node": "^24.0.0",
    "sigstore": "5.0.0",
    "typescript": "7.0.2",
    "vscode-languageserver-protocol": "^3.17.5",
    "vscode-languageserver-textdocument": "^1.0.12",
    "vscode-languageserver-types": "^3.18.0",
    "yaml": "^2.9.0"
  },
  "//devDependencies": "Workspace packages are dev dependencies so root examples and tests resolve them by name without creating build-order edges. Resolution probes must account for this root node_modules route as well as member-local routes. yaml is also needed by staged builds that borrow only root node_modules entries.",
  "//name": "Keep the private workspace name distinct from published member names; Bun permits a root/member name collision without warning.",
  "//private": "This root is never published. Release checks inspect each publishable member manifest."
}
